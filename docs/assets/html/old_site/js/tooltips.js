$(document).ready(function() {
    // $('label[for="AV:N"]').tooltip({title: "", delay: {show: 500, hide: 100}});
    $('label[for="AV:N"]').tooltip({title: "A vulnerability exploitable with network access means the vulnerable component is bound to the network stack and the attacker's path is through OSI layer 3 (the network layer). Such a vulnerability is often termed &quot;remotely exploitable” and can be thought of as an attack being exploitable one or more network hops away.", placement: "bottom", delay: {show: 500, hide: 100}});
    $('label[for="AV:A"]').tooltip({title: "A vulnerability exploitable with adjacent network access means the vulnerable component is bound to the network stack, however the attack is limited to the same shared physical (e.g. Bluetooth, IEEE 802.11), or logical (e.g. local IP subnet) network, and cannot be performed across an OSI layer 3 boundary (e.g. a router).", placement: "bottom", delay: {show: 500, hide: 100}});
    $('label[for="AV:L"]').tooltip({title: "A vulnerability exploitable with local access means that the vulnerable component is not bound to the network stack, and the attacker’s path is via read/write/execute capabilities. In some cases, the attacker may be logged in locally in order to exploit the vulnerability, otherwise, she may rely on User Interaction to execute a malicious file.", placement: "bottom", delay: {show: 500, hide: 100}});
    $('label[for="AV:P"]').tooltip({title: "A vulnerability exploitable with physical access requires the attacker to physically touch or manipulate the vulnerable component. Physical interaction may be brief or persistent.", placement: "bottom", delay: {show: 500, hide: 100}});
    $('label[for="AC:L"]').tooltip({title: "Specialized access conditions or extenuating circumstances do not exist. An attacker can expect repeatable success against the vulnerable component.", placement: "bottom", delay: {show: 500, hide: 100}});
    $('label[for="AC:H"]').tooltip({title: "A successful attack depends on conditions beyond the attacker's control. That is, a successful attack cannot be accomplished at will, but requires the attacker to invest in some measurable amount of effort in preparation or execution against the vulnerable component before a successful attack can be expected. For example, a successful attack may require the attacker: to perform target-specific reconnaissance; to prepare the target environment to improve exploit reliability; or to inject herself into the logical network path between the target and the resource requested by the victim in order to read and/or modify network communications (e.g. a man in the middle attack).", delay: {show: 500, hide: 100}});
    $('label[for="PR:N"]').tooltip({title: "The attacker is unauthorized prior to attack, and therefore does not require any access to settings or files to carry out an attack.", placement: "bottom", delay: {show: 500, hide: 100}});
    $('label[for="PR:L"]').tooltip({title: "The attacker is authorized with (i.e. requires) privileges that provide basic user capabilities that could normally affect only settings and files owned by a user. Alternatively, an attacker with Low privileges may have the ability to cause an impact only to non-sensitive resources.", placement: "bottom", delay: {show: 500, hide: 100}});
    $('label[for="PR:H"]').tooltip({title: "The attacker is authorized with (i.e. requires) privileges that provide significant (e.g. administrative) control over the vulnerable component that could affect component-wide settings and files.", placement: "bottom", delay: {show: 500, hide: 100}});
    $('label[for="UI:N"]').tooltip({title: "The vulnerable system can be exploited without any interaction from any user.",placement: "bottom", delay: {show: 500, hide: 100}});
    $('label[for="UI:R"]').tooltip({title: "Successful exploitation of this vulnerability requires a user to take some action before the vulnerability can be exploited.", placement: "bottom", delay: {show: 500, hide: 100}});
    $('label[for="S:U"]').tooltip({title: "An exploited vulnerability can only affect resources managed by the same authority. In this case the vulnerable component and the impacted component are the same.", placement: "bottom", delay: {show: 500, hide: 100}});
    $('label[for="S:C"]').tooltip({title: "An exploited vulnerability can affect resources beyond the authorization privileges intended by the vulnerable component. In this case the vulnerable component and the impacted component are different.", placement: "bottom", delay: {show: 500, hide: 100}});
    $('label[for="C:N"]').tooltip({title: "There is no loss of confidentiality within the impacted component.", placement: "bottom",delay: {show: 500, hide: 100}});
    $('label[for="C:L"]').tooltip({title: "There is some loss of confidentiality. Access to some restricted information is obtained, but the attacker does not have control over what information is obtained, or the amount or kind of loss is constrained. The information disclosure does not cause a direct, serious loss to the impacted component.", placement: "bottom", delay: {show: 500, hide: 100}});
    $('label[for="C:H"]').tooltip({title: "There is total loss of confidentiality, resulting in all resources within the impacted component being divulged to the attacker. Alternatively, access to only some restricted information is obtained, but the disclosed information presents a direct, serious impact.", placement: "bottom", delay: {show: 500, hide: 100}});
    $('label[for="I:N"]').tooltip({title: "There is no loss of integrity within the impacted component.", placement: "bottom",delay: {show: 500, hide: 100}});
    $('label[for="I:L"]').tooltip({title: "Modification of data is possible, but the attacker does not have control over the consequence of a modification, or the amount of modification is constrained. The data modification does not have a direct, serious impact on the impacted component.", placement: "bottom", delay: {show: 500, hide: 100}});
    $('label[for="I:H"]').tooltip({title: "There is a total loss of integrity, or a complete loss of protection. For example, the attacker is able to modify any/all files protected by the impacted component. Alternatively, only some files can be modified, but malicious modification would present a direct, serious consequence to the impacted component.", placement: "bottom", delay: {show: 500, hide: 100}});
    $('label[for="A:N"]').tooltip({title: "There is no impact to availability within the impacted component.",placement: "bottom", delay: {show: 500, hide: 100}});
    $('label[for="A:L"]').tooltip({title: "There is reduced performance or interruptions in resource availability. Even if repeated exploitation of the vulnerability is possible, the attacker does not have the ability to completely deny service to legitimate users. The resources in the impacted component are either partially available all of the time, or fully available only some of the time, but overall there is no direct, serious consequence to the impacted component.", delay: {show: 500, hide: 100}});
    $('label[for="A:H"]').tooltip({title: "There is total loss of availability, resulting in the attacker being able to fully deny access to resources in the impacted component; this loss is either sustained (while the attacker continues to deliver the attack) or persistent (the condition persists even after the attack has completed). Alternatively, the attacker has the ability to deny some availability, but the loss of availability presents a direct, serious consequence to the impacted component (e.g., the attacker cannot disrupt existing connections, but can prevent new connections; the attacker can repeatedly exploit a vulnerability that, in each instance of a successful attack, leaks a only small amount of memory, but after repeated exploitation causes a service to become completely unavailable).", delay: {show: 500, hide: 100}});
});